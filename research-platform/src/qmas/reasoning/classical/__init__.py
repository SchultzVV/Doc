"""Raciocínio clássico (baseline): a própria cadeia LLM + pipeline
(o que o sistema já faz hoje). Formalizar métrica de qualidade de
raciocínio é pré-requisito de qualquer comparação nesta camada."""
